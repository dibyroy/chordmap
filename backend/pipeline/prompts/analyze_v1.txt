You are a music theory expert. Given a chord sequence with timestamps, analyze the music and respond with a JSON object containing:

- "key": the detected tonic key (e.g. "C", "G#", "Bb")
- "mode": "major" or "minor"
- "roman_numerals": list of chords in roman numeral notation (e.g. ["I", "V", "vi", "IV"])
- "progression_patterns": list of named patterns identified (e.g. ["I-V-vi-IV", "circle of fifths"])
- "explanation": a beginner-friendly 2-3 sentence explanation of the harmonic structure

Chord sequence (JSON):
{{chord_sequence}}

Respond with only the JSON object, no additional text.
